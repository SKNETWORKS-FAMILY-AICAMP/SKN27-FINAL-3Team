### 2026-07-02 작업 업데이트

#### 한 일
- 이의가능성 판단 에이전트 설계문서(`설계문서/이의가능성_판단_에이전트_설계정리.md`) 리뷰 및 보완
  - `LDB_CHECK`가 `CHK_REASON`보다 먼저 실행되어 `law_code_verified`가 이미 계산된 상태로
    `input_required` 응답을 보내는 순서 문제 발견 및 반영
  - `input_required`의 `partial_result`에 `law_code_verified`를 함께 실어 보내도록 v13 도식·
    설계 포인트 수정 (기존 "타임라인 포함" 원칙과 동일하게 통일)
  - RG(리스크 분류)·MG(인정사유 분류) 판단 방식·근거 재확인

#### 할 일
- 이의가능성 판단 에이전트 구현 착수 (`ai/agents/appeal_decision_flow/`)
  - `ST`/`DCHK`(타임라인·기한도과 게이트), `LDB_CHECK`, `CHK_REASON` 순서대로 노드 구현
  - `RG`(2단계: 키워드 → LLM), `MG`(LLM 단일 호출, notice_stage별 컨텍스트 분기) 구현
  - `E` 노드 — RG×MG 조합 매트릭스 기반 출력 통합
  - `input_required` 응답에 `partial_result`(타임라인 + `law_code_verified`) 포함하는 로직 구현

#### 막힌 점 / 확인 필요
- 별표28(벌점 기준표)이 법령DB API에서 구조화 텍스트/뭉개진 텍스트/이미지 중 어떤 형태로
  오는지 미확인 (`ZP`, 별표28 매핑 관련 — 설계문서 미결 사항 B그룹 7·8번 참고)

#### 관련 PR / 문서 / 이슈
- [설계문서/이의가능성_판단_에이전트_설계정리.md](설계문서/이의가능성_판단_에이전트_설계정리.md)
- 브랜치: `feat/fine-appeal-decision-flow`
