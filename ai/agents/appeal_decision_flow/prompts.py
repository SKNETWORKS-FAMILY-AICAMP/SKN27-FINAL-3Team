RISK_CLASSIFICATION_PROMPT = """\
당신은 과태료 이의신청 사유의 "신원노출 위험"을 판단하는 전문가입니다.
아래 사유 텍스트를 보고, 이 사유를 이의신청서에 쓸 경우 실제 운전자의 신원이
드러나 범칙금으로 전환될 위험이 있는지 판단하여 JSON만 반환하세요.

**판단은 category 하나로만 내리세요.** 별도의 위험 여부 판단을 따로 하지 마세요 —
category가 A/B/C 중 하나로 정해지면 그 자체가 "위험 있음"이고, null이면 "위험 없음"
입니다. 두 개를 독립적으로 판단하면 서로 모순되는 답이 나올 수 있으니 category
판단 하나에만 집중하세요.

먼저 이 사유가 "차량 도난·절도"로 인한 것인지 확인하세요 — 도둑맞은 차를 도둑이
운전하다가 위반한 경우는 성공(도난 입증)·실패(입증 못함) 어느 쪽이든 아무에게도
범칙금 전환 위험이 실현되지 않으므로 무조건 안전합니다. 도난에 해당하면
category=null로 응답하세요.

도난이 아니라면 아래 세 카테고리 중 해당하는 게 있는지 확인하세요. **단어가 일치하는지가
아니라 요청·진술의 의도가 해당하는지로 판단하세요** — 같은 의도를 다른 단어로 표현해도
같은 카테고리입니다.
- A(제3자 운전 주장): 소유자가 아닌 다른 특정 인물(지인·가족 등)이 운전했다는 주장
- B(명시적 전환 요청): 과태료 대신 범칙금(통고처분·즉결심판 등 형사절차 성격의 처리)으로
  바꿔달라는 요청. "범칙금"이라는 단어를 쓰지 않고 "통고처분"·"정식 절차"·"그쪽으로
  처리해달라"처럼 다르게 표현해도, 결과적으로 과태료 처분을 범칙금 계열 처리로 전환해
  달라는 의도이면 B에 해당합니다.
- C(본인 운전 인정형): 본인이 위반 당시 운전자였음을 전제로, 부득이한 사유나
  정상참작 요소(응급환자 이송, 장애인 승하차 등)를 설명하는 진술

해당하는 카테고리가 없으면 category=null로 응답하세요.

예시 (참고용 — 표현이 달라도 의도가 같으면 같은 category를 반환하세요):
- "범칙금으로 전환해주세요" → B
- "이 건은 과태료가 아니라 통고처분 절차로 넘겨서 처리해 주십시오" → B (단어는 다르지만
  같은 의도)
- "다른 사람이 운전했습니다" → A
- "고지서 금액이 잘못 계산됐습니다" → null (신원과 무관)

사유: {reason}

반환 필드:
- category   : "A" | "B" | "C" | null
- confidence : "high" | "medium" | "low"
- rationale  : 판단 근거 한 줄 요약

규칙:
- JSON 코드블록 없이 순수 JSON만 출력. 설명 문장 없이 JSON만 반환.
"""

MERIT_CLASSIFICATION_PROMPT = """\
당신은 과태료 이의신청 사유가 법적으로 인정될 만한지 판단하는 전문가입니다.
아래 참조 법조문을 유일한 판단 기준으로 삼아, 사용자의 사유가 이 조문에 해당하는지
판단하여 JSON만 반환하세요. 참조 법조문에 없는 내용을 근거로 판단하지 마세요.

--- 참조 법조문 ---
{law_context}
--- 참조 법조문 끝 ---

사유: {reason}

판단 기준:
- "강함": 참조 법조문에 명확히 해당하는 경우. **사유 문장에 "응급"·"위독"·"긴급" 같은
  강조 표현이 없어도, 서술된 사실관계 자체가 열거된 사유(예: 다친/아픈 사람을 병원으로
  데려가기 위한 위반)에 해당하면 강함으로 판단하세요** — 판단 기준은 강조어의 유무가
  아니라 사실관계가 조문 요건에 실질적으로 해당하는지입니다. 예: "위독한 환자를 이송했다"와
  "다친 사람을 병원까지 데려다줬다"는 강조어만 다를 뿐 같은 사실관계이므로 같은 판정을
  받아야 합니다.
- "보류": 열거 목록엔 없으나 귀책없음 취지이거나 정황요소 해당 가능성이 있는 경우
- "낮음": 참조 법조문과 명백히 무관한 경우

반환 필드:
- merit       : "강함" | "보류" | "낮음"
- merit_basis : 인용한 조문·근거 한 줄 요약

규칙:
- JSON 코드블록 없이 순수 JSON만 출력. 설명 문장 없이 JSON만 반환.
"""

# merit="강함"일 때만 별도로 호출한다 (RG가 is_identity_related+category를 한 번에
# 물었다가 모순 응답이 나와서 category 하나로 쪼갠 것과 반대 방향의 같은 교훈 — 판단
# 하나당 호출 하나로 쪼개면 정확도가 오른다). 같은 호출 안에서 merit과 함께 물었을 때
# "판단능력이 미약했다"는 사유에도 계속 "면제"(완전 무능력)로 잘못 답하는 게 실측으로
# 확인돼 분리했다 — "면제·감경 사유 merit 구분 설계.md" §7 참고.
RELIEF_TYPE_CLASSIFICATION_PROMPT = """\
당신은 과태료 사유가 인정될 경우 그 법적 효과가 "처분 전액 면제"인지 "처분 유지·금액
감경"인지만 판단하는 전문가입니다. 이 사유는 이미 참조 법조문에 해당한다고(merit=강함)
판정됐습니다 — 그 판정을 다시 검토하지 말고, 오직 면제인지 감경인지만 판단하세요.

--- 참조 법조문 ---
{law_context}
--- 참조 법조문 끝 ---

사유: {reason}

**절차 — 반드시 이 순서로 판단하세요:**
1. 먼저 참조 법조문에서 이 사유가 정확히 어느 조·어느 항에 해당하는지 특정하세요.
2. **그 특정한 항의 법률 효과만 그대로 따르세요** — 그 항의 문구가 "부과하지 아니한다"면
   면제, "감경한다"면 감경입니다. **사유 텍스트에 "다소/약간/일부/미약"류 표현이
   있다는 이유만으로 감경을 선택하면 안 됩니다** — 참조 법조문 자체에 감경을 규정한
   항이 없다면(예: 시행규칙 제142조, 질서법 제7·8·9조, 도로교통법 제160조4항1호는
   전부 "부과하지 아니한다"만 있고 감경 항이 아예 없음) 사유 표현이 어떻든 무조건
   면제입니다.
3. **예외적으로, 같은 조문 안에 정도가 다른 항이 함께 있는 경우에만**(예: 질서법
   제10조 — ①"능력이 없는 경우"=면제, ②"능력이 미약한 경우"=감경) 사유 텍스트가
   완전한 불능을 말하는지(①) 부분적 저하를 말하는지(②) 구분해서 판단하세요. 이때
   사유가 완전한 불능·무능력을 명시하지 않고 조금이라도 정도를 낮춰 말했다면(예:
   "다소", "약간", "미약") 면제가 아니라 감경입니다.

판단이 애매하면 항상 더 보수적인 쪽(감경)을 선택하세요 — 처분이 취소될 거라는
과도한 기대를 사용자에게 주면 안 됩니다. 다만 이건 "정도가 다른 항이 실제로 함께
있는 조문"에서만 해당하는 얘기지, 감경 항 자체가 없는 조문까지 보수적으로 낮추라는
뜻이 아닙니다.

반환 필드:
- relief_type   : "면제" | "감경"
- matched_clause: 실제로 해당한다고 판단한 조·항 표기 (예: "제10조②", "제142조5호")
- rationale     : 그 항의 법률 효과와 판단 근거 한 줄 요약

규칙:
- JSON 코드블록 없이 순수 JSON만 출력. 설명 문장 없이 JSON만 반환.
"""
