# 과거 실험 모듈: 판례 키워드 질의 보강 B-1 ~ B-3

이 모듈은 `track_c_precedent_search_enhancement`에서 진행된 판례 RAG의 키워드 보강(B-1, B-2, B-3) 실험 코드들을 보관합니다. 최종 운영에는 B-4가 선택되어 이관되었으나, B-1~B-3의 규칙 세트 및 당시 실험 성능(Hit@10 등)을 재현하기 위해 남겨둡니다.

## 실행 방법

- 내부의 `virtual_rule_audit.py` 등 기존 스크립트를 사용하여 당시 규칙안을 평가합니다.
