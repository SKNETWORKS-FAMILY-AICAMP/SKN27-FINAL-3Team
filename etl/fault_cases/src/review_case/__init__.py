"""Review case collection package.

This package currently keeps the crawling entry points and shared collection
helpers. Preprocessing code has been reset so it can be rebuilt separately
under ``review_case/preprocessing`` later.
"""

