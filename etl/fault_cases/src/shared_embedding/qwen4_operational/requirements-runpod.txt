# PyTorch 2.6 이상은 torch.load 보안 제한과 Qwen3 실행 호환성을 위해 고정한다.
torch==2.7.1
# Qwen3 임베딩의 SentenceTransformer 로딩·정규화 실행 버전이다.
sentence-transformers==5.0.0
# Qwen3 모델 구현을 지원하는 Transformers 버전이다.
transformers==4.54.0
# 고정 commit 조회와 모델 다운로드에 사용하는 Hub 클라이언트다.
huggingface-hub==0.34.3
# Transformers 4.54 계열과 호환되는 tokenizer 범위를 고정한다.
tokenizers>=0.21.0,<0.22.0
# 벡터 shard와 최종 결과 Parquet 생성에 사용한다.
pyarrow>=20.0.0,<22.0.0
# 안전한 모델 가중치 형식을 명시적으로 지원한다.
safetensors>=0.5.3,<1.0.0
# RunPod 기본 이미지에 남은 구형 pandas/scikit 계열 바이너리와의 ABI 충돌을 막는다.
numpy==1.26.4
