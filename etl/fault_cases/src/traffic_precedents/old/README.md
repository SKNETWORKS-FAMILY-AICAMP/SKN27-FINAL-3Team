# 구 판례 파이프라인 보관본

이 디렉터리는 판례 RAG 교체 과정에서 기존 구현을 임시 보관한다.

- 활성 코드에서 `traffic_precedents.old`를 import하지 않는다.
- 런타임 fallback으로 사용하지 않는다.
- 신형 파이프라인과 검색의 인수 테스트가 끝나면 삭제할 수 있다.
- 생성 JSONL, NPY, 압축 파일, 캐시와 실행 로그는 보관하지 않는다.
