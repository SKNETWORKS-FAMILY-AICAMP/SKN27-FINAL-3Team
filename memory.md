# Project Memory

작성일: 2026-06-18

## 현재 상태

- 저장소는 구현 코드보다 문서/설계 산출물이 중심인 초기 단계다.
- 현재 브랜치는 `docs-mvp-screen-and-process-flows`이며 HEAD는 `origin/dev`의 초기 커밋 `f3ff6ae`와 같다.
- 현재 작업 트리에는 `docs/screen-design-specification.md`, `docs/assets/`, `output/`, `tmp/`가 미추적 상태로 존재한다.
- `docs-project-scope-and-role-matrix` 브랜치만 `origin/dev` 대비 1개 커밋 앞서 있으며, 요구사항 정의서 v0.4와 기존 역할 매트릭스가 들어 있다.

## 운영 맥락

- 프로젝트명은 `교통분쟁 AI: 과실비율·범칙금/과태료 분석 및 리포팅 서비스`다.
- 중간 발표 기준일은 2026-07-14, 최종 마무리 기준일은 2026-08-04로 문서화되어 있다.
- 요구사항 정의서 v0.4 기준 주요 축은 인증/마이페이지/과거이력/챗봇/리포트/데이터 파이프라인/RAG/ML/DL/Agent/Backend/DB/Ops다.
- 기존 문서 기준 추천 RAG 구조는 MVP 단계의 단일 RAG 저장소이며, `domain`, `source_type`, `metadata`로 데이터를 구분한다.

## 구조


| 경로         | 현재 의미             |
| ---------- | ----------------- |
| `docs/`    | 문서 작성 공간          |
| `app/`     | 웹/앱 서비스 공간        |
| `ai/`      | ML/DL 또는 파인튜닝 공간  |
| `etl/`     | 데이터 수집/처리 공간      |
| `storage/` | 저장소 정의 공간, docker |
| `test/`    | 테스트 또는 스터디 공간     |
| `output/`  | 생성된 PDF/XLSX 산출물  |
| `tmp/`     | 산출물 생성/검증 스크립트    |


## 확인된 브랜치


| 브랜치                                  | 상태                                                 |
| ------------------------------------ | -------------------------------------------------- |
| `dev`                                | `origin/dev`와 동일, 초기 README 골격                     |
| `docs-mvp-screen-and-process-flows`  | 현재 브랜치, `origin/dev`와 동일 커밋 위에 미추적 화면설계 산출물 존재     |
| `docs-project-scope-and-role-matrix` | `origin/dev` 대비 1개 커밋 앞섬, 요구사항/역할/AI-Hub 리포트 문서 포함 |
| `docs-requirement-gap-and-risk-log`  | `origin/dev`와 동일, 별도 커밋 없음                         |
| `docs-wbs-owner-deliverable-plan`    | `origin/dev`와 동일, 별도 커밋 없음                         |


## 기존 역할 기준

- 기존 `docs/project-scope-and-role-matrix.md` 기준:
  - `hi20260204-maker`: 역할/범위 문서, WBS/산출물 문서, MVP 화면/프로세스 문서
  - `leejaegang27`: 홈 진입점, RAG chunk/index 일부
  - `ohjuheecode`: 분석 Job 모델, RAG chunk/index 일부
  - `workzion2`: 데이터 출처 저장 구조, 증분 수집, schema 분리, RAG chunk/index 일부
  - `techshin31`: 요구사항 Gap/Risk 문서 공동 담당
- `tmp/pdfs/generate_wbs_assignment_pdf.py` 기준의 후속 배정안:
  - `hi20260204-maker`: 보험 영상 수집/전처리, DB/Django/AWS 배포, 인증인가, 문서/QA 총괄
  - `leejaegang27`: 보험 텍스트 ML/RAG, 과실비율 지식베이스/응답 정책
  - `ohjuheecode`: 보험 영상 DL/RAG, Vision/DL POC, 작업 상태 모델
  - `techshin31`: 보험 RAG Agent/Front, 홈/챗봇/리포트 화면, DL 결과 검토 연결
  - `workzion2`: 범칙금 RAG Agent/Front로만 정정. 기존 스크립트에 있던 보험 텍스트 수집/전처리 파이프라인 표기는 현재 회의 기준과 맞지 않으므로 재강 담당으로 이동.

## 2026-06-18 회의 변경 영향

- 새 회의안은 기존 배정안을 크게 변경한다.
- 큰 도메인 구조는 사건 전/후로 나뉜다.
  - 사건 전: 혜림 담당으로 제안됨.
  - 사건 후: 과태료/범칙금 분석 흐름은 필주 담당으로 확정.
  - 사건 후: 과실비율은 재강, 주희 담당으로 제안됨.
- 데이터/Agent 분해:
  - 텍스트 이미지: 재강
  - 경위서/OCR 결과 처리: 재강
  - 텍스트 ML 모델 학습/베이스라인: 재강
  - 고지서 OCR 및 과태료·범칙금 분석 흐름: 필주
  - 차량 사고 이미지 및 영상: 주희
  - 경위서 관련 Agent: 텍스트 이미지 기반, 재강
  - 법률 데이터 수집/전처리/DB 적재: 동혁
  - 법률 Agent 구현 담당은 별도 확정 필요
  - 판례 Agent: 재강
  - 영상/이미지 Agent: 주희
  - 이의신청서 생성 Agent 또는 문서 생성 흐름: 사용자(요청자) 담당으로 확정
  - 보험 약관 데이터: 후순위로 진행하며 이번 역할/WBS 병목에서는 제외
- UI 변경:
  - 홈 화면 + 로그인 페이지 구성
  - 로그인은 모달 방식
  - 로그인 후 바로 챗봇 페이지로 이동
  - 챗봇 응답은 상세 설명보다 간편한 설명 중심
- 그래프 변경:
  - 메인 챗봇 개발자가 분기 조절
  - 슈퍼바이저 Agent가 과실비율, 범칙금, 자동차 관련 법률 질문을 분기
  - 데이터 타입별로 분석 흐름을 분리
  - 각 Agent별 상황에 따라 그래프를 작성하고, 최종적으로 하나의 메인 그래프로 합칠 필요가 있음
  - 노드 하나에 하위 그래프를 삽입하는 구조가 필요함

## 충돌 및 확정 필요 사항

- 기존 요구사항은 `UI-CHAT-001` 범칙금 챗봇과 `UI-CHAT-002` 보험 챗봇을 분리하지만, 화면설계서는 `UI-Ai-01` 단일 AI 교통 상담 챗봇으로 통합한다.
- 기존 요구사항은 기존 사용자가 로그인 후 메인 또는 마이페이지로 이동한다고 되어 있으나, 새 회의안은 로그인 후 바로 챗봇 페이지로 이동한다.
- 기존 홈 담당은 문서에 따라 `leejaegang27` 또는 `techshin31`로 다르게 기록되어 있다.
- 새 회의안의 한국어 이름과 기존 GitHub 계정 매핑은 명시 확정이 필요하다.
- 보험 약관 Agent와 11개 보험사 약관 데이터 수집은 후순위로 진행한다.
- 필주 담당 과태료·범칙금 분석 흐름은 고지서 OCR 처리, OCR 결과 검토/구조화, 위반 정보 구조화, 처분 단계, 이의제기 가능성, 부족 서류/필요 증거 판단까지 포함한다.
- 필주 담당 데이터 범위에는 과태료, 범칙금, 벌칙 데이터가 포함된다.
- 필주의 과태료·범칙금·벌칙 데이터는 분석용 룰/매핑 데이터로 보고, 동혁의 법률 데이터는 원문 조항과 근거 metadata를 보존하는 법률 근거 DB로 분리해야 중복 작업을 줄일 수 있다.
- 판례/법률 쪽 파인튜닝 여부는 확정 필요하나, 개별 Agent는 근거/분석 결과 스키마를 반환하고 최종 자연어 답변은 Supervisor가 통합 생성하는 방향으로 둔다.
- 동혁 담당 법률 데이터는 판례가 아니라 법령/시행령/시행규칙/행정 기준 등 법률 계열 데이터의 수집, 전처리, DB 적재까지 포함하는 것으로 정정되었으므로 `REQ-DATA-PEN`, `REQ-RAG`, `REQ-BE`, `REQ-DB`와 연결해야 한다.
- 최종 답변 생성은 개별 담당자의 단일 Agent가 아니라 최종 Supervisor가 각 Agent의 결과 스키마를 보고 통합 답변을 형성하는 구조로 둔다.
- 이의신청서 생성은 사용자(요청자) 담당으로 확정한다. 과태료/범칙금 결과, 법률 근거, 사용자 입력을 받아 문서 초안을 생성하는 별도 Report 노드로 분리한다.
- 재강 담당 ML 모델은 `REQ-ML-001~006`과 연결된다. 범위는 텍스트 청크 생성, 임베딩 생성, 사고 유형 분류, 요약 생성, 키워드/태그 생성, ML 결과의 RAG metadata 반영으로 제한하는 것이 안전하다.
- 재강 담당 경위서/OCR 결과 처리는 OCR 원문을 최종 판단으로 쓰는 것이 아니라, 사고 상황 텍스트를 정제하고 사고유형/쟁점/증거 태그를 만드는 ML/RAG 입력 전처리 단계로 두는 것이 안전하다.
- 재강 ML 모델을 과실비율 판례, 유튜브 자막 사례, 과실비율심의사례 데이터를 바탕으로 분류값, 요약값, 태그값, 유사도/추천값을 산출하는 모델로 두면 흐름상 자연스럽다.
- 재강 ML 모델을 최종 법률 답변 생성 또는 과실비율 확정 판단 모델로 정의하면 동혁 법률 데이터 적재, 재강 판례 Agent, 주희 영상 Agent와 책임 경계가 흐려지므로 피해야 한다.
- 판례/법률 텍스트 학습은 기존 프로젝트 문서의 영상 DL 흐름과 별도이므로 그 자체가 주희 영상/DL 병목을 만들지는 않는다. 단, 영상 분석 결과까지 같은 모델의 필수 입력으로 묶으면 병목이 생길 수 있다.
- 영상/이미지 분석은 DL, 그 외 텍스트는 ML/RAG 흐름으로 가는 방향이 제안되었으나, 실제 모델 학습 범위와 베이스라인 대체 범위는 확정 필요하다.

## 다음 작업

1. 새 회의안을 반영한 역할 매트릭스를 확정한다.
2. `docs-project-scope-and-role-matrix`의 역할 문서를 최신 배정안으로 갱신한다.
3. `docs-wbs-owner-deliverable-plan` 또는 신규 문서에 다음주 금요일까지의 WBS를 재작성한다.
4. 요구사항 정의서에 로그인 후 챗봇 이동, 로그인 모달, 슈퍼바이저 그래프, 사건 전/후 도메인 분기를 반영할지 확정한다.
5. LangGraph 기준 메인 그래프, 과태료/범칙금 서브그래프, 과실비율 서브그래프, 법률/판례/경위서/영상/이의신청서 생성 Agent 노드 경계와 Supervisor 통합 답변 스키마를 문서화한다.
6. 재강 ML 모델의 입력 데이터셋, 라벨 체계, 출력 metadata 계약을 판례/경위서/보험 텍스트 RAG와 맞춘다.
7. API/DB/RAG 계약이 새 그래프 구조를 지원하는지 점검한다.

## 2026-06-18 GitHub Issue/WBS 동기화 결과

- `docs/wbs-owner-deliverable-plan.md`를 새 WBS 원문으로 작성했다.
- GitHub Issues `#1`~`#43`의 제목, 본문, assignee, label, milestone을 최신 역할 기준으로 동기화했다.
- `#6`, `#34`, `#35`는 합의금 관련 scope-out 이슈로 닫았다. 영구 삭제는 하지 않았다.
- `#11 docs-wbs-owner-deliverable-plan` 본문에는 새 WBS 원문 전체를 업로드했다.
- 생성/설정된 milestone:
  - `2026-07-14 중간 발표 MVP`
  - `2026-08-04 최종 마무리`
- GitHub Project 보드는 현재 인증 토큰에 `read:project` 또는 classic token의 전체 제어용 `project` scope가 없어 연결하지 못했다. fine-grained token이면 Projects 권한을 Read and write로 부여해야 한다.
- `#44 test 업무`는 닫힌 테스트 이슈로 남아 있으며, 영구 삭제는 명시 확인 후 진행해야 한다.
- 2026-06-18 추가 정정:
  - `write:project` scope 표현은 부정확했다. classic token 기준 GitHub Project 전체 제어는 `project`, 읽기는 `read:project`가 맞다. fine-grained token은 Projects 권한을 Read and write로 설정해야 한다.
  - 현재 토큰 scope는 `gist, repo, workflow`로 확인되었다.
  - GitHub Issues `#1`~`#43`에 `<!-- wbs-meeting-update-2026-06-18 -->` 마커가 있는 상세 회의 반영 코멘트를 생성했다.
  - parent/child 구조를 정리했다. `#1`은 `#9` 아래로, `#22`와 `#29`는 `#3` 아래로 이동했다.
  - 오늘 회의록 문서로 `docs/meeting-notes-2026-06-18-role-wbs-update.md`를 추가했다. 권장 브랜치는 `docs-wbs-owner-deliverable-plan`이다.

## 최신 역할 상세

### 혜림

- 담당 영역: 사건 전 영역과 초기 사용자 흐름.
- 담당 목적: 사용자가 서비스를 처음 진입했을 때 어떤 정보를 입력하고 어떤 분석 흐름으로 넘어갈지 정리한다.
- 입력 데이터:
  - 사용자 초기 질문
  - 홈 화면 또는 로그인 이후 진입 정보
  - 사고 발생 전 정보
  - 사용자가 제공하는 기본 사고 상황
  - 챗봇 시작 전에 필요한 기본 입력값
- 상세 책임:
  - 홈 화면에서 챗봇 또는 진단 흐름으로 넘어가는 초기 플로우를 정리한다.
  - 로그인 이후 바로 챗봇 페이지로 이동하는 흐름과 사건 전 입력 흐름을 연결한다.
  - 사건 전 단계에서 사용자에게 받아야 할 기본 질문 항목을 정의한다.
  - 사고 전 상황, 사고 유형 후보, 사용자 의도 분류에 필요한 입력값을 정리한다.
  - 메인 챗봇/슈퍼바이저 그래프가 과태료·범칙금, 과실비율, 법률 질문, 영상/이미지 분석으로 분기할 때 필요한 초기 조건을 정리한다.
- 산출물:
  - 사건 전 사용자 플로우
  - 초기 입력 항목 정의
  - 챗봇 진입 전후 화면/그래프 연결 기준
  - 슈퍼바이저 분기 조건 초안
- 다른 담당과의 연결:
  - 필주에게 과태료·범칙금 분석으로 넘길 입력 조건을 제공한다.
  - 재강에게 경위서/OCR/텍스트 분석으로 넘길 사고 설명 텍스트 조건을 제공한다.
  - 주희에게 이미지/영상 분석이 필요한 파일 입력 조건을 제공한다.

### 필주

- 담당 영역: 고지서 OCR 및 과태료·범칙금 분석 흐름.
- 담당 목적: 고지서 이미지 또는 OCR 결과를 바탕으로 과태료·범칙금 사건을 구조화하고, 처분 단계와 이의제기 가능성 판단에 필요한 분석 결과를 만든다.
- 입력 데이터:
  - 고지서 이미지
  - 고지서 OCR 원문
  - OCR 결과에서 추출된 위반 일시, 위반 장소, 위반 유형, 통지일, 납부기한, 관할 기관
  - 납부 여부
  - 처분 단계 정보
  - 사용자 추가 설명
  - 동혁이 적재한 법률 데이터 검색 결과
  - 과태료 데이터
  - 범칙금 데이터
  - 벌칙 데이터
- 상세 책임:
  - 고지서 OCR 처리 흐름을 담당한다.
  - OCR 결과를 검토하고 위반 정보 필드로 구조화한다.
  - OCR 신뢰도 또는 필드 누락 여부를 확인한다.
  - 위반 유형, 통지일, 납부기한, 관할 기관 등 과태료·범칙금 분석에 필요한 핵심 값을 정리한다.
  - 과태료, 범칙금, 벌칙 데이터를 분석 흐름에서 참조할 수 있게 정리한다.
  - 현재 처분 단계가 의견진술 전인지, 과태료 부과 예정인지, 납부 이후인지 등을 판단할 수 있도록 상태값을 만든다.
  - 이의제기 가능성 판단에 필요한 부족 서류와 필요 증거를 정리한다.
  - 동혁의 법률 데이터와 연결해서 관련 법령 또는 행정 기준 근거를 참조할 수 있게 한다.
  - 이의신청서 생성 노드가 사용할 수 있도록 분석 결과를 정해진 구조로 넘긴다.
- 산출물:
  - 고지서 OCR 구조화 결과
  - 과태료·범칙금 분석 결과
  - 처분 단계 상태값
  - 이의제기 가능성 판단값
  - 부족 서류 목록
  - 필요 증거 목록
  - 이의신청서 생성 입력 데이터
- 다른 담당과의 연결:
  - 동혁의 법률 데이터 적재 결과를 근거로 사용한다.
  - 요청자 담당 이의신청서 생성 Agent/Report 노드에 분석 결과를 넘긴다.
  - 혜림의 사건 전 플로우에서 과태료·범칙금 흐름으로 분기된 입력을 받는다.

### 동혁

- 담당 영역: 법률 데이터 수집, 전처리, DB 적재.
- 담당 목적: 판례가 아닌 법률 계열 데이터를 수집하고 정제해서 과태료·범칙금 분석과 법률 근거 검색에 사용할 수 있는 DB/RAG 입력 데이터로 만든다.
- 입력 데이터:
  - 도로교통법
  - 도로교통법 시행령
  - 도로교통법 시행규칙
  - 범칙금·과태료 관련 법령 조항
  - 행정 기준
  - 관련 고시 또는 규칙성 데이터
  - 법률 OpenAPI 또는 공개 원문
- 제외 범위:
  - 판례 데이터 수집은 동혁 담당이 아니다.
  - 판례 Agent 구현은 동혁 담당이 아니다.
- 상세 책임:
  - 법률 데이터의 수집 출처를 확정한다.
  - 법률 원문을 수집한다.
  - 조문, 항, 호, 별표, 금액, 벌점, 적용 대상, 예외 조건을 구조화한다.
  - HTML, 불필요한 문구, 중복, 깨진 텍스트를 전처리한다.
  - 법률 데이터에 `domain`, `source_type`, `law_name`, `article`, `category`, `effective_date`, `source_url` 같은 metadata를 붙인다.
  - DB 적재용 schema에 맞게 데이터를 변환한다.
  - DB에 실제 적재한다.
  - 적재 건수, 실패 건수, 누락 필드, 중복 여부를 검증한다.
  - RAG 문서 등록이 필요한 경우 법률 문서 chunk와 summary를 생성할 수 있게 준비한다.
- 산출물:
  - 법률 원천 데이터 목록
  - 전처리된 법률 데이터
  - DB 적재 결과
  - RAG 등록용 법률 문서
  - 적재 검증 로그
  - 법률 데이터 README 또는 파이프라인 실행 문서
- 다른 담당과의 연결:
  - 필주의 과태료·범칙금 분석 흐름에 법률 근거 데이터를 제공한다.
  - 요청자 담당 이의신청서 생성 노드에 법률 근거 데이터를 제공한다.
  - 재강의 판례/텍스트 ML 결과와 최종 근거 단계에서 병합될 수 있다.

### 재강

- 담당 영역: 경위서/OCR 결과 처리, 텍스트 ML 모델 학습/베이스라인, 판례 Agent.
- 담당 목적: 경위서와 OCR 결과 텍스트, 판례/사고 사례 텍스트를 정제하고 ML 기반으로 분류, 요약, 태그, 유사도 또는 추천값을 산출해서 과실비율/판례 검색 흐름의 근거 데이터로 만든다.
- 입력 데이터:
  - 사고 경위서 텍스트
  - 경위서 이미지에서 나온 OCR 결과
  - 고지서 OCR 결과 중 사고 설명 또는 사용자 설명에 해당하는 텍스트
  - 판례 데이터
  - 보험 사고 사례 텍스트
  - 유튜브 자막 또는 사고 설명 텍스트
  - 과실비율 판례 데이터
  - 유튜브 자막 기반 과실비율 사례 데이터
  - 과실비율심의사례 데이터
  - 동혁이 적재한 법률 데이터 중 근거 병합에 필요한 metadata
- 상세 책임:
  - 경위서/OCR 결과 텍스트를 정제한다.
  - OCR 결과의 깨진 문장, 중복 문장, 불필요한 문구를 제거한다.
  - 사고 설명 텍스트에서 사고 유형 후보를 분류한다.
  - 사고 쟁점, 증거 유형, 과실 쟁점, 신호/차선/진로 관련 태그를 생성한다.
  - 판례/사고 사례 텍스트를 chunk 단위로 나눈다.
  - 과실비율 판례, 유튜브 자막 사례, 과실비율심의사례 데이터를 ML/RAG 입력 형태로 정제한다.
  - 텍스트 임베딩을 생성한다.
  - 요약문을 생성한다.
  - ML 결과를 RAG 문서의 `summary`, `metadata`, `tags`에 반영한다.
  - 판례 Agent가 사용할 수 있는 검색/응답 흐름을 만든다.
  - 판례 검색 결과가 최종 법률 판단처럼 보이지 않도록 근거/참고자료 형태로 반환한다.
- ML 범위:
  - 텍스트 청크 생성
  - 임베딩 생성
  - 사고 유형 분류
  - 요약 생성
  - 키워드/태그 생성
  - 유사 판례 또는 유사 사고 사례 추천값 산출
  - ML 결과의 RAG metadata 반영
- 제외 또는 주의 범위:
  - 과실비율을 확정 수치로 단정하는 모델은 만들지 않는다.
  - 최종 법률 답변 생성 모델로 정의하지 않는다.
  - 주희의 영상 분석 결과가 반드시 있어야만 재강 ML이 실행되는 구조로 묶지 않는다.
- 산출물:
  - 경위서/OCR 정제 결과
  - 사고 유형 분류 결과
  - 쟁점/증거 태그
  - 요약문
  - 임베딩 또는 RAG 등록 결과
  - 판례 Agent 응답 결과
  - ML 베이스라인 실행 코드 또는 파이프라인
  - ML 결과 검증 샘플
- 다른 담당과의 연결:
  - 혜림의 사건 전 입력 흐름에서 받은 사고 설명 텍스트를 처리한다.
  - 필주의 고지서/OCR 결과 중 텍스트 분석이 필요한 부분을 받을 수 있다.
  - 주희의 영상/이미지 분석 결과와 최종 과실비율 흐름에서 병합된다.
  - 동혁의 법률 데이터와 판례/법률 근거 병합 단계에서 연결된다.

### 주희

- 담당 영역: 차량 사고 이미지 및 영상, 영상·이미지 Agent, Vision/DL 흐름.
- 담당 목적: 사고 사진, 차량 이미지, 블랙박스 또는 사고 영상을 분석해서 사고 전후 장면, 주요 프레임, 영상 요약, 신뢰도 정보를 만들고 과실비율 흐름에서 활용할 수 있게 한다.
- 입력 데이터:
  - 사고 현장 사진
  - 차량 파손 이미지
  - 블랙박스 영상
  - 사고 영상
  - 영상에서 추출한 프레임
  - AI-Hub 또는 샘플 비전 데이터셋
  - 영상 metadata
- 상세 책임:
  - 이미지/영상 데이터셋을 수집하거나 정리한다.
  - 영상 파일 metadata를 생성한다.
  - 영상에서 분석 가능한 프레임을 추출한다.
  - 사고 전후 구간을 나눈다.
  - 주요 장면 또는 key frame을 추출한다.
  - 차량, 차선, 신호등, 충돌 시점 등 분석 가능한 요소를 식별한다.
  - 영상/이미지 분석 결과를 텍스트 요약으로 변환한다.
  - 분석 결과에 confidence 또는 정성 신뢰도 라벨을 붙인다.
  - 영상 분석 결과를 RAG 저장 또는 Agent 근거로 넘길 수 있는 구조로 만든다.
- 산출물:
  - 이미지/영상 데이터셋 목록
  - 영상 metadata
  - 추출 프레임
  - 주요 장면 정보
  - 영상 분석 요약문
  - confidence 또는 신뢰도 라벨
  - 영상/이미지 Agent 응답 결과
  - Vision/DL 베이스라인 결과
- 다른 담당과의 연결:
  - 혜림의 초기 파일 입력 흐름에서 영상/이미지 분석 대상으로 분기된다.
  - 재강의 텍스트 ML/판례 결과와 과실비율 흐름에서 병합된다.
  - 동혁 법률 데이터와 함께 최종 근거 설명에 사용될 수 있다.

### 요청자

- 담당 영역: 이의신청서 생성 Agent 또는 Report 노드.
- 담당 목적: 필주의 과태료·범칙금 분석 결과와 동혁의 법률 근거를 받아 실제 사용자가 참고할 수 있는 이의신청서 초안을 생성하는 흐름을 담당한다.
- 입력 데이터:
  - 필주의 고지서 OCR 구조화 결과
  - 필주의 과태료·범칙금 분석 결과
  - 필주의 이의제기 가능성 판단값
  - 필주의 부족 서류/필요 증거 목록
  - 동혁의 법률 근거 데이터
  - 사용자 사실관계
  - 사용자 추가 설명
- 상세 책임:
  - 이의신청서에 들어갈 항목 구조를 정의한다.
  - 수신 기관, 제목, 사건 정보, 위반 정보, 사실관계, 이의신청 취지, 근거, 첨부 증거, 주의 문구 구조를 잡는다.
  - 법률 근거와 사용자 사실관계를 분리해서 문서에 반영한다.
  - AI가 사실을 단정하거나 승소/성공 가능성을 보장하지 않도록 제한 문구를 넣는다.
  - 초안 생성 프롬프트 또는 템플릿을 만든다.
  - 복사/다운로드/리포트 저장에 필요한 출력 구조를 정의한다.
- 산출물:
  - 이의신청서 초안 생성 노드
  - 이의신청서 템플릿 또는 프롬프트
  - 문서 출력 구조
  - 면책/주의 문구
  - Report 노드 결과 schema
- 다른 담당과의 연결:
  - 필주의 분석 결과가 선행되어야 한다.
  - 동혁의 법률 데이터가 선행되어야 한다.
  - 사용자 추가 입력이 부족하면 추가 질문을 요청해야 한다.

### 후순위

- 보험 약관 Agent와 11개 보험사 약관 데이터 수집은 이번 주요 역할/WBS 병목에서 제외한다.
- 보험 약관 데이터는 과실비율 고도화 또는 보험사 사례 보강 단계에서 후순위로 진행한다.


## 최신 그래프 흐름 초안

```text
메인 챗봇/슈퍼바이저
→ 입력 유형 및 질문 의도 분류
→ 사건 전 흐름(혜림)
→ 사건 후 분기
   → 과태료·범칙금 분석 흐름(필주)
      → 법률 데이터 검색/근거 연결(동혁 데이터)
      → 이의신청서 생성 Agent/Report 노드(사용자)
   → 과실비율 흐름
      → 경위서/OCR/텍스트 ML/판례 Agent(재강)
      → 차량 사고 이미지/영상 Agent(주희)
      → 법률 데이터 검색/근거 연결(동혁 데이터)
→ Supervisor가 각 Agent 결과 스키마를 통합해 최종 답변, 리포트, 후속 행동 제시
```

## 2026-06-18 주간회의 및 이슈 완성도 문서 추가

- `docs/260618_주간회의.md`를 추가했다.
  - 기준 자료는 `260618_회의록_original.txt`, 사용자 수기 회의록, 기존 WBS 문서다.
  - 핵심 내용은 고지서 OCR과 법률 Agent 연결 판단, LangGraph/Supervisor 의미, 여러 AI 모델 비교 활용, 화면설계서/API 명세 조사 방식, 금요일 전 산출물이다.
- `docs/260618_이슈_완성도.md`를 추가했다.
  - GitHub Issues `#1`~`#44`의 상태, 담당자, milestone, 댓글 수, 현재 브랜치 커밋 현황을 기준으로 작성했다.
  - 진행도는 기능 구현 완료율이 아니라 이슈 관리/문서화/커밋 근거 기준의 추정치로 명시했다.
  - 현재 브랜치 기준 기본 브랜치 대비 추가 커밋은 2개, 변경 파일은 58개, 추가 라인은 8,766 lines다.
  - 커밋 메시지에 `#이슈번호`가 직접 포함된 커밋은 없어 이슈별 직접 연결 커밋 수는 0개로 기록했다.

## 2026-06-19 폴더 구조 및 dev 반영 이슈 확인

- 현재 원격 `dev`는 `f3ff6aec796654ac41d5435fe9e1d6680e20a1c8` 초기 커밋 상태이며, 루트 골격은 `README.md`, `app/`, `ai/`, `docs/`, `etl/`, `storage/`, `test/` README 중심이다.
- 현재 브랜치 `docs-wbs-owner-deliverable-plan`은 `origin/dev` 대비 4개 커밋 앞서 있으며, 추가 내용은 주로 WBS, 화면설계, 이슈 템플릿, PDF/output, GitHub 동기화 스크립트다.
- GitHub Issue 최신 조회 기준으로 `폴더 구조`, `프로젝트 구조`, `scaffold`를 직접 제목으로 다루는 전용 이슈는 없다.
- 폴더 구조 작업을 기존 이슈에 연결한다면 가장 가까운 후보는 아래 순서다.
  - `#3 epic-common-architecture-data-pipeline`: 공통 아키텍처와 데이터 파이프라인 parent issue.
  - `#18 feat-separate-domain-case-schemas`: 도메인별 schema 분리와 책임 경계.
  - `#16 feat-data-source-registry-schema`: 데이터 출처 registry와 원문 위치 metadata.
  - `#22 feat-agent-result-schema-and-rag-contract`: Agent 결과 schema와 RAG evidence 계약.
- 다만 구현 문서에는 실제 패키지/폴더 세부 구조가 명시되어 있지 않다. 따라서 `app/ai/etl/storage/test` 하위 구조를 임의로 만들거나 `dev`에 병합하기 전에는 별도 확인이 필요하다.
- `docs/260619_내부회의.md` 기준으로 구현/문서 커밋은 회의 결정 후 진행하며, 구현 착수용 세부 문서와 커밋/푸시는 각 이슈 브랜치 또는 문서 파일에서 회의 결정 후 진행하도록 기록되어 있다.

## 2026-06-22 PDF 제출 산출물 생성

- 사용자 요청에 따라 현재 프로젝트 문서와 제공 양식 기준으로 PDF 3종을 생성했다.
- 최종 PDF 위치:
  - `output/pdf/프로젝트_기획서_27기_3팀_2026-06-22.pdf`
  - `output/pdf/수집_데이터_보고서_27기_3팀_2026-06-22.pdf`
  - `output/pdf/화면설계서_27기_3팀_2026-06-22.pdf`
- 제공된 DOCX 양식 기반 중간 산출물:
  - `output/docx/프로젝트_기획서_27기_3팀_2026-06-22.docx`
  - `output/docx/수집_데이터_보고서_27기_3팀_2026-06-22.docx`
- 검증 결과:
  - `tmp/document_generation/verify_generated_pdfs.py`로 페이지 수와 핵심 키워드 추출을 확인했다.
  - 프로젝트 기획서 2쪽, 수집 데이터 보고서 3쪽, 화면설계서 5쪽으로 확인됐다.
  - Poppler `pdftoppm.exe`로 PNG 렌더링 후 대표 페이지를 시각 확인했고, 한글 깨짐/겹침/빈 페이지 문제는 확인되지 않았다.
- 남은 리스크:
  - 저장소 안에 실제 raw 데이터 파일이 확인되지 않는 항목은 보고서에서 진행 중 또는 검증 필요로 표기했다.
  - 로컬에 LibreOffice `soffice`가 없어 DOCX 렌더러 기반 PDF 변환은 실패했고, 최종 PDF는 ReportLab 직접 생성 방식으로 만들었다.
  - `pdfinfo` 콘솔 출력에서 PDF 메타데이터 한글은 깨져 보일 수 있으나, 본문 렌더링과 텍스트 추출은 정상 확인됐다.

## 2026-06-22 Decision: #43 표준 운영 준비형

### Options

| 선택 | 방향 | 판정 |
|---|---|---|
| A | 문서·이슈 close 정리형 | 선택하지 않음 |
| B | 표준 운영 준비형 | 선택됨 |
| C | 정적 프론트 MVP 강화형 | 선택하지 않음 |
| D | Agent 계약 골격형 | 선택하지 않음 |

### Decision

사용자는 `B. 표준 운영 준비형`을 선택했다. 작업 대상 이슈는 `#43 chore-final-stabilization-and-release-readiness`이며, 작업 브랜치는 `chore-final-stabilization-and-release-readiness`다.

### Constraints

- 표준 운영 준비형 기준으로 배포 준비성 검토, 운영 절차, 정적 검증 테스트를 먼저 만든다.
- 실제 Google 로그인, Agent, RAG, API endpoint, 모델 선택은 구현하지 않는다.
- 팀원 schema 없이 `#22`, `#27`, `#29`, `#40`, `#41`을 완료 처리하지 않는다.
- close 가능성은 검증 결과와 사용자 승인 후에만 판정한다.

## 2026-07-21 프론트 컬러 팔레트 (Dynamic Black / Honey Beige / Egg Liqueur / Apocalyptic Orange)

- `app/web/styles.css`의 `:root`에 적용된 전체 앱 컬러 팔레트. 새 화면/컴포넌트 작업 시 하드코딩 hex 대신 이 변수를 참조한다.
- 핵심 4색:
  - Dynamic Black → `--ink: #161415`
  - Honey Beige → `--soft` / `--page-bg: #efe0c2`
  - Egg Liqueur → `--line: #d6ce9e`
  - Apocalyptic Orange → `--brand: #f76a21` (RGB 247, 106, 33)
- 파생/보조 톤:
  - `--brand-dark: #c54607`, `--brand-soft: #feede4`, `--brand-soft-border: #fdd2bc`
  - `--surface: #ffffff`, `--ink-soft: #4a4643`, `--muted: #6b665f`, `--subtle: #948d7f`
  - `--info: #285f9f` / `--info-soft: #e9f0fa`
  - `--green: #1f7a6d` / `--green-soft: #e7f4f1`
  - `--amber: #a15c00` / `--amber-soft: #fff4df`
  - `--red: #b42318` / `--red-soft: #fff0ed`
- radius 스케일: `--radius-sm: 8px`, `--radius-md: 12px`(기본 카드/버튼), `--radius-lg: 16px`, `--radius-pill: 999px`.
- 주의: 예전 홈 히어로 디자인 때 쓰던 `.insurance-layout`/`.layout.is-entry .workspace` 중복 선언이 이 팔레트를 조용히 덮어쓴 적이 있었음(거의 흰색 `#fffdfb`/`#fffdfa`로 override) — 배경 관련 새 규칙 추가 시 파일 뒤쪽에 동일 셀렉터 중복 선언이 없는지 먼저 grep으로 확인할 것.
