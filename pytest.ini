[pytest]
addopts = --strict-markers
markers =
    unit: isolated tests without network or service dependencies
    integration: tests that cross application or persistence boundaries
    live: tests that call paid or external APIs and require --run-live
    aws: tests that use provisioned AWS resources and require --run-aws

